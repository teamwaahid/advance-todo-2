<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login</title>
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0-beta1/dist/css/bootstrap.min.css"
    />
    <link rel="stylesheet" href="/css/signup.css" />
  </head>

  <body>
    <div class="container">
      <div class="signup">
        <h1 class="text-center text-uppercase">Login</h1>
        <form method="post" action="/login">
          <div class="mb-3">
            <label for="exampleInputEmail1" class="form-label"
              >Your Email</label
            >

            <input
             name="email" 
             type="email" 
             class="form-control 
            <%= err && emailError ? "border-danger" : '' %> 
            " 
            id="exampleInputEmail1"
            aria-describedby="emailHelp"
             <% if(err){ %> 
               value="<%=email%>" 
             <% } %>
              /> 
              <% if(err && emailError ){ %>
                 <p class="text-danger"><%= err %></p>
             <% } %>
          </div>

          <div class="mb-3">
            <label for="exampleInputPassword1" class="form-label"
              >Password</label
            >
            <input
            name="password"
              type="password"
              class="form-control 
              <%= passwordErr? "border-danger" : '' %> "
              id="exampleInputPassword1"
            />
            <% if(err && passwordErr){ %> 
              <p class="text-danger"> <%= err %> </p>
            <% } %> 
          </div>

          <button type="submit" class="btn w-100 btn-primary">Login</button>
        </form>
        <p class="text-center">New user? <a href="/signup">signup</a></p>
      </div>
    </div>
  </body>
</html>
